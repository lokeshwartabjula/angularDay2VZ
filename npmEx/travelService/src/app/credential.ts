//This is the interface created to encapsulate a credentials object because we need an object for authentication in login form component

export interface Credential {
    userName : string; 
    passWord : string;
    
}
